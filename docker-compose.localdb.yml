services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: acrn_adjudication
      POSTGRES_USER: acrn_user
      POSTGRES_PASSWORD: acrn_local_password
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U acrn_user -d acrn_adjudication"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_NAME: acrn_adjudication
      DB_USER: acrn_user
      DB_PASSWORD: acrn_local_password
      DB_HOST: db
      DB_PORT: "5432"
      # SSL between Docker containers is unnecessary (same internal network)
      DB_SSL_MODE: disable
      ENVIRONMENT: development
      ENABLE_DEMO_ACCOUNTS: "true"
      AUTH_COOKIE_SECURE: "false"
      SECRET_KEY: local-dev-secret-change-me

volumes:
  pgdata:
